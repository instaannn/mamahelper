# Настройка оплаты через ЮKassa (с поддержкой СБП)

## Шаг 1: Регистрация в ЮKassa

1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru/)
2. Создайте магазин и получите доступ к личному кабинету

## Шаг 2: Получение учетных данных

1. Войдите в личный кабинет ЮKassa
2. Перейдите в раздел "Настройки" → "API"
3. Скопируйте:
   - **Shop ID** (идентификатор магазина)
   - **Секретный ключ** (Secret Key)

**⚠️ ВАЖНО для СБП:**
- **Для работы СБП нужны БОЕВЫЕ ключи**, а не тестовые
- В тестовом режиме СБП может быть недоступен
- Убедитесь, что используете ключи из раздела "Боевой режим", а не "Тестовый режим"
- Если у вас только тестовые ключи, СБП может не работать

## Шаг 3: Настройка переменных окружения

Добавьте в файл `.env`:

```bash
YOOKASSA_SHOP_ID=your_shop_id_here
YOOKASSA_SECRET_KEY=your_secret_key_here
```

## Шаг 4: Установка зависимостей

```bash
pip install -r requirements.txt
```

Библиотека `yookassa` будет установлена автоматически.

## Шаг 5: Активация способов оплаты в ЮKassa

1. В личном кабинете ЮKassa перейдите в "Настройки" → "Способы оплаты"
2. Активируйте нужные способы оплаты:
   - **Банковские карты** (Visa, Mastercard, Maestro, Мир) - активированы по умолчанию
   - **СБП (Система быстрых платежей)** - для оплаты через QR-код
   - Другие способы по необходимости
3. Сохраните изменения

**Важно:** 
- Оплата картами работает автоматически, если аккаунт активирован
- **СБП нужно активировать отдельно** в настройках
- После активации СБП в личном кабинете, он автоматически появится на странице оплаты

**Если СБП не появляется, проверьте:**

1. **Используете ли вы БОЕВЫЕ ключи?**
   - В тестовом режиме СБП может быть недоступен
   - Убедитесь, что `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` - это боевые ключи из раздела "Боевой режим"
   - Тестовые ключи обычно содержат слово "test" в названии

2. **Активирован ли СБП в личном кабинете?**
   - Перейдите в "Настройки" → "Способы оплаты"
   - Убедитесь, что "СБП (Система быстрых платежей)" включен
   - Сохраните изменения

3. **Прошел ли аккаунт модерацию?**
   - Некоторые способы оплаты требуют дополнительной модерации аккаунта
   - Проверьте статус аккаунта в личном кабинете

4. **Проверьте логи бота:**
   - При запуске бот покажет, в каком режиме работает (тестовый/боевой)
   - Если видите "ТЕСТОВЫЙ режим", СБП может быть недоступен

5. **Если все проверено, но СБП все еще не появляется:**
   - Обратитесь в поддержку ЮKassa
   - Укажите, что СБП активирован в личном кабинете, но не появляется на странице оплаты

## Шаг 6: Настройка Webhook (опционально)

### ⚠️ ВАЖНО: Webhook НЕ обязателен!

**Без webhook все тоже работает!** Бот автоматически проверяет платежи каждые 5 минут и активирует премиум. Webhook нужен только для мгновенной активации (вместо ожидания до 5 минут).

### Что такое публичный HTTPS URL?

Это адрес вашего сервера в интернете, например:
- `https://your-domain.com` (если у вас есть домен)
- `https://123.45.67.89` (если у вас только IP-адрес)

**Требования:**
- ✅ Должен быть доступен из интернета (не localhost)
- ✅ Должен использовать HTTPS (не HTTP)
- ✅ Должен быть настроен SSL-сертификат

### Откуда взять публичный HTTPS URL?

#### Вариант 1: У вас есть домен и сервер
Если у вас есть домен (например, `your-domain.com`) и сервер:
1. Настройте nginx или другой reverse proxy
2. Проксируйте запросы на `http://localhost:8080/webhooks/yookassa`
3. Настройте SSL-сертификат (Let's Encrypt)
4. URL будет: `https://your-domain.com/webhooks/yookassa`

#### Вариант 2: У вас только IP-адрес сервера
Если у вас есть только IP-адрес (например, `123.45.67.89`):
1. Нужно настроить SSL-сертификат для IP (сложнее)
2. Или использовать сервисы типа ngrok (для тестирования)

#### Вариант 3: Нет публичного URL (рекомендуется для начала)
**Просто не используйте webhook!** Это нормально и работает:
- Бот проверяет платежи каждые 5 минут
- Премиум активируется автоматически
- Ничего дополнительного настраивать не нужно

### Вариант 1: С webhook (если есть публичный HTTPS URL)

1. **Включите webhook сервер в боте:**
   Добавьте в файл `.env`:
   ```bash
   YOOKASSA_WEBHOOK_ENABLED=true
   WEBHOOK_HOST=0.0.0.0
   WEBHOOK_PORT=8080
   ```

2. **Пробросьте порт в docker-compose.yml:**
   ```yaml
   services:
     bot:
       # ... остальные настройки ...
       ports:
         - "8080:8080"  # Добавьте эту строку
   ```

3. **Настройте nginx/reverse proxy** (если используете):
   ```nginx
   location /webhooks/yookassa {
       proxy_pass http://localhost:8080;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
   }
   ```

4. **Настройте webhook в личном кабинете ЮKassa:**
   - Перейдите в "Настройки" → "HTTP-уведомления"
   - Добавьте URL: `https://your-domain.com/webhooks/yookassa`
   - Выберите события: `payment.succeeded`
   - Сохраните

5. **Опционально: настройте проверку подписи:**
   - В личном кабинете ЮKassa найдите секретный ключ для webhook
   - Добавьте в `.env`:
     ```bash
     YOOKASSA_WEBHOOK_SECRET=your_webhook_secret_key
     ```

### Вариант 2: Без webhook (рекомендуется, если нет публичного URL)

**Просто оставьте настройки по умолчанию:**
```bash
YOOKASSA_WEBHOOK_ENABLED=false  # или не указывайте эту переменную
```

**Как это работает:**
- ✅ Бот автоматически проверяет платежи каждые 5 минут
- ✅ Премиум активируется при следующей проверке
- ✅ Пользователь может вернуться в бот после оплаты (`/start payment_success`) - премиум активируется сразу
- ✅ Ничего дополнительного настраивать не нужно

**Преимущества:**
- Не нужен публичный URL
- Не нужно настраивать SSL
- Не нужно настраивать nginx
- Все работает автоматически

**Недостаток:**
- Активация может занять до 5 минут (но обычно быстрее, так как пользователь возвращается в бот)

## Как это работает

1. Пользователь нажимает кнопку покупки премиума
2. Бот создает платеж через API ЮKassa
3. Пользователь получает ссылку на оплату
4. Пользователь переходит по ссылке на страницу оплаты ЮKassa
5. На странице оплаты пользователь может выбрать способ оплаты:
   - 💳 **Банковская карта** (Visa, Mastercard, Maestro, Мир)
   - 📱 **СБП** (Система быстрых платежей) - если активировано
   - Другие способы оплаты (если активированы)
6. После успешной оплаты:
   - **С webhook:** ЮKassa отправляет уведомление → бот активирует премиум мгновенно
   - **Без webhook:** Бот проверяет платежи каждые 5 минут → активирует премиум автоматически
   - **Быстрая активация:** Если пользователь возвращается в бот после оплаты (`/start payment_success`) → премиум активируется сразу, не дожидаясь периодической проверки

## Тестирование

Для тестирования используйте тестовые данные:
- Тестовые карты: https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing
- Тестовый режим в личном кабинете

## Дополнительная информация

- [Документация API ЮKassa](https://yookassa.ru/developers/payment-acceptance/getting-started/payment-process)
- [Python SDK](https://yookassa.ru/developers/using-api/using-sdks#server-sdk)
- [Интеграция с Telegram](https://git.yoomoney.ru/projects/SUP/repos/other/browse/telegram_integration.md)

