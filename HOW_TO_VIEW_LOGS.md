# Как посмотреть логи бота

## 📝 Где находятся логи

Логи записываются в файл: **`app/data/bot.log`**

Также логи выводятся в консоль (терминал), где запущен бот.

## 🔍 Способы просмотра логов

### 1. Просмотр файла логов (рекомендуется)

```bash
# Посмотреть последние 50 строк
tail -n 50 app/data/bot.log

# Посмотреть последние 100 строк с обновлением в реальном времени
tail -f app/data/bot.log

# Посмотреть весь файл
cat app/data/bot.log

# Поиск ошибок
grep -i error app/data/bot.log

# Поиск ошибок команды /start
grep -i "start" app/data/bot.log | grep -i error
```

### 2. Просмотр в консоли

Если бот запущен в терминале, логи выводятся прямо в консоль.

### 3. Если бот запущен в Docker

```bash
# Посмотреть логи контейнера
docker-compose logs -f bot

# Посмотреть последние 100 строк
docker-compose logs --tail=100 bot

# Поиск ошибок
docker-compose logs bot | grep -i error
```

## 🐛 Что искать в логах при ошибке /start

При ошибке команды `/start` ищите строки с:
- `❌ КРИТИЧЕСКАЯ ОШИБКА в команде /start`
- `Error in start command`
- `Traceback` или `Exception`

## 📋 Пример команды для быстрого просмотра последних ошибок

```bash
# Посмотреть последние ошибки
tail -n 200 app/data/bot.log | grep -A 10 -i "error\|exception\|traceback"
```

## 💡 Полезные команды

```bash
# Очистить логи (осторожно!)
> app/data/bot.log

# Посмотреть размер файла логов
ls -lh app/data/bot.log

# Найти все упоминания конкретного user_id
grep "user_id=123456789" app/data/bot.log
```

