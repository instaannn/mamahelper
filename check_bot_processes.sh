#!/bin/bash
# Скрипт для проверки и завершения запущенных процессов бота

echo "🔍 Проверяю запущенные процессы бота..."

# Ищем процессы Python, которые запускают наш бот
PROCESSES=$(ps aux | grep -E "python.*app\.main|python.*bot\.py" | grep -v grep)

if [ -z "$PROCESSES" ]; then
    echo "✅ Запущенных процессов бота не найдено."
    exit 0
fi

echo "⚠️  Найдены запущенные процессы бота:"
echo "$PROCESSES"
echo ""
echo "Для завершения процессов выполните:"
echo "$PROCESSES" | awk '{print "kill " $2}'
echo ""
echo "Или завершите все процессы одной командой:"
echo "$PROCESSES" | awk '{print $2}' | xargs kill





